{
  "name": "coding-problem-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Strictly typed TypeScript workspace for coding problems.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate": "npm run build --silent && node dist/src/cli.js generate",
    "solve": "npm run build --silent && node dist/src/cli.js solve",
    "judge": "npm run build --silent && node dist/src/cli.js judge",
    "test": "npm run build --silent && node --test dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22"
  }
}
